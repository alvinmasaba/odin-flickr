<%= form_with url: "/", method: :get do |form| %>
  <%= form.text_field :user_id, {:prompt => "Enter User ID"} %>
  <%= form.submit "Search User" %>
<% end %>

<% if photos %>
Photos
    <% photos.each do |photo_url| %>
      <div><%= image_tag photo_url %></div>
    <% end %>
<% end %>
